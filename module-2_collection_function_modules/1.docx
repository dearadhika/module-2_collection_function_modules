what is list?
~> a list is a collection of items that can be of any data type, including strings, integers, floats, and other.
~> list is a mutable, odered, allow duplicate values, indexed, sliced.
~> list can be assigned in a [] brackets.

how will you reverse a list?
~> using reverse method we can reverse a list.

num = [1, 2, 3, 4, 5]
num.reverse()
print(num)  
Output is => [5, 4, 3, 2, 1]

